# Ignore everything by default
*

# But keep this gitignore so the rule itself is tracked
!.gitignore

# Track only the top-level `src/` directory and its contents
!/WebGame/
!/WebGame/**
!BackgroundImage.jp
!/README.md
# (All other files/folders are intentionally ignored)
